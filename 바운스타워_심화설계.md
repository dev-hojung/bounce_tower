# ⚪ 바운스 타워 — 심화 설계 v0.1

> 4개 영역 심화: ①레벨 생성 알고리즘 ②콤보·무적·점수 ③비주얼 아이덴티티·연출 ④메타·수익화
> 상위 문서: 물리아케이드_기획서.md, 바운스타워_상세설계.md

> ⚠️ **문서 상태 (2026-08-14)**
> ①②③(레벨생성·콤보/무적·비주얼)은 **그대로 유효**하며 현재 코드의 근거 문서다.
> 단 용어 하나만 바뀌었다 — ①②의 **"젬 픽업/수집"은 실제로 코인 발판으로 구현**됐다
> (`runBonusCoins`). 하드 커런시가 아니라 런 중에 줍는 보너스 코인이다.
> **④ 메타·수익화는 폐기·대체됨** — 젬/IAP/배틀패스는 도입하지 않고 **광고 전용**으로
> 확정했다. 현행 사양은 [바운스타워_제품화설계.md](바운스타워_제품화설계.md)를 볼 것.

---

# ① 레벨 생성 알고리즘 (절차적 생성 + 난이도 곡선)

## 1.1 설계 철학
- 타워는 **무한**이지만 무작위가 아니라 **"난이도 곡선 기반 절차적 생성(PCG)"**.
- 핵심 원칙: **읽을 수 있는 무작위**. 플레이어가 "운 나빠서 죽었다"가 아니라 "내가 못 피했다"고 느껴야 한다.
- 난이도는 **속도가 아닌 레이아웃**으로 올린다(상세설계 문서 기조 유지).

## 1.2 링(ring) 데이터 모델
한 링 = N개 슬롯(보통 N=8~12). 각 슬롯 상태:
| 상태 | 의미 |
|------|------|
| `SAFE` | 밟으면 튕김 (기본) |
| `GAP` | 빈 틈 (통과 가능) |
| `DANGER` | 위험 (무적 아니면 사망) |
| `BONUS` | 젬/부스터 (선택) |

한 링 생성 규칙:
- **GAP ≥ 1** 항상 보장 (통과 불가능한 링 금지 → "운으로 죽음" 방지).
- GAP는 **연속 슬롯 묶음**으로 (1개 점이 아니라 호(arc) 형태여야 통과 가능).
- DANGER는 GAP에 **인접 배치 가능**하되, GAP 폭 ≥ 최소통과폭 유지.

## 1.3 난이도 곡선 (데이터화)
깊이(depth)를 입력으로 파라미터를 계산하는 **곡선 함수**로 관리. 하드코딩 금지 → 원격설정(Firebase Remote Config)으로 출시 후에도 튜닝.

```
difficulty(depth):
    gapArc      = lerp(220°, 70°, clamp(depth/60))   // 틈이 점점 좁아짐
    dangerRatio = lerp(0.05, 0.45, clamp(depth/80))   // 위험 비율 증가
    gapCount    = depth < 15 ? 2 : 1                   // 초반 틈 2개 → 1개
    fallSpeed   = lerp(1.0, 1.4, clamp(depth/100))     // 살짝만 증가
```
- `clamp(x)` = min(1, x). 곡선은 전부 **포화(saturate)** 되게 → 무한히 어려워지지 않고 "고난도 평탄구간"으로 수렴(숙련자 장기 플레이 가능).

## 1.4 페이싱 (리듬 — 긴장과 이완)
연속 고난도는 피로감. **구간 패턴**을 섞는다:
- **호흡 구간**: 5~8링마다 쉬운 링(넓은 틈, DANGER 0) → 콤보 리셋·숨 고르기.
- **러시 구간**: 좁은 틈 + DANGER 밀집 → 콤보 무적으로 뚫는 카타르시스 구간.
- **보너스 구간**: 젬 다수 배치 → 보상 수확.
→ 시퀀스: `[온보딩]→호흡→점증→러시→호흡→보너스→…` 반복(랜덤 셔플).

## 1.5 시드 & 데일리
- 엔드리스: 매 판 랜덤 시드.
- **데일리 런**: 날짜를 시드로 → 전 세계 동일 타워 → 친구와 공정 비교(랭킹 핵심).
- 시드 기반 생성이므로 **서버 검증·리플레이**도 가능(치트 방지 토대).

## 1.6 검증 체크
- 생성된 링이 **항상 통과 가능**한지 단위 테스트(GAP 존재 + 폭 ≥ 최소).
- "운 사망률"을 로그로 측정 → 너무 높으면 곡선 완화.

---

# ② 콤보 · 무적 · 점수 시스템

## 2.1 콤보 = 코어 루프의 깊이
- **콤보 정의**: 안전 조각을 밟지 않고 **GAP만 연속 통과**한 링 수.
- 콤보가 임계치(예: 3링 연속)에 도달하면 **무적(Rage) 발동**.

## 2.2 무적(Rage) 상태
- 발동 시: 공이 **빛나며 가속 낙하**, DANGER·SAFE를 **부수며 관통**.
- 지속: 시간제(예: 1.5초) 또는 거리제(예: 5링).
- 종료: 부드럽게 감속 복귀. (갑자기 멈추면 사망 위험 → 복귀 후 0.3초 유예)
- **쾌감의 정점** = 화면 파괴 + 점수 폭발. 이 순간이 **바이럴 클립**이 된다.

## 2.3 점수 공식
```
score += baseRingScore × comboMultiplier × rageBonus

comboMultiplier = 1 + floor(combo / 3) × 0.5   // 3콤보마다 +0.5배
rageBonus       = isRage ? 2.0 : 1.0
perfectGapBonus = (틈 정중앙 통과 시) +α        // 정밀 보상
```
- 점수원: ①깊이(생존) ②콤보 배율 ③무적 관통 ④젬 수집.
- **깊이만으로도 점수가 오르되, 콤보를 노리면 폭발** → 안전 플레이/공격 플레이 둘 다 유효(플레이스타일 선택권).

## 2.4 위험-보상 설계
- 콤보를 쌓으려면 SAFE를 안 밟고 좁은 틈만 노려야 함 = **위험**.
- 무적으로 보상 = **카타르시스**.
- → "안전하게 깊이 vs 과감하게 콤보" 의사결정이 매 순간 생김 = 깊이.

## 2.5 피드백 (체감)
| 이벤트 | 피드백 |
|--------|--------|
| GAP 통과 | 짧은 '핑' + 작은 파티클 |
| 콤보 누적 | 숫자 팝업 커짐, 색 변화, 진동 약 |
| 무적 발동 | 화면 채도↑, BGM 필터, 강한 진동 |
| 무적 관통 | 슬로모 살짝 + 파편 폭발 + 카메라 흔들림 |
| 사망 | 정지 프레임 + 공 산산조각 |

---

# ③ 비주얼 아이덴티티 · 연출 (레드오션 탈출 핵심)

## 3.1 왜 중요한가
- 메커닉은 Helix Jump와 유사 → **"어디서 본 것"을 벗어나는 건 비주얼·무드**.
- 하이퍼캐주얼 성패의 큰 축 = **광고 크리에이티브에서 첫 1초의 시각적 매력**.

## 3.2 아이덴티티 후보 (택1, 일관 적용)
| 무드 | 설명 | 광고 매력 |
|------|------|-----------|
| **A. 네온 사이버** | 다크 배경 + 네온 글로우 + 트레일 | 발광 연출 → 클립 화려 |
| **B. 젤리/구미** | 말랑한 파스텔 + 탱탱한 스쿼시 물리 | 귀여움 → 광클릭 |
| **C. 미니멀 모노** | 흑백+1포인트컬러, 절제미 | 세련/고급, 차별 강함 |
| **D. 자연/유기체** | 물·꿀·용암 등 유체 느낌 낙하 | ASMR성 만족감 |
> 추천: **B(젤리)** 또는 **A(네온)** — 숏폼에서 가장 잘 먹힘. 최종은 광고 테스트로 결정.

## 3.3 "주스(Juice)" 연출 — 손맛의 정체
하이퍼캐주얼 체감의 80%는 미세 연출의 누적:
- **스쿼시 & 스트레치**: 공이 튕길 때 눌렸다 늘어남.
- **카메라**: 충돌 시 미세 흔들림, 무적 시 약간 줌아웃.
- **파티클**: 통과·파괴마다 색 파편.
- **트레일**: 공 뒤 잔상(특히 무적).
- **히트스톱**: 강한 충돌 순간 1~2프레임 정지 → 타격감.
- **이징(easing)**: 모든 UI·연출에 ease-out. 선형 금지.
- **색 반응 배경**: 깊이/콤보에 따라 배경 그라데이션 변화.

## 3.4 접근성 (글로벌 필수)
- 색만으로 위험 구분 금지 → **형태/패턴 병기**(가시 모양 등).
- 진동·화면흔들림·플래시 **토글** 제공(광과민성 배려).

## 3.5 사운드 (권리 안전)
- SFX: 튕김·통과·콤보·파괴·사망 각각 명확히. **무적 시 사운드 고조**.
- BGM: 로열티프리 루프, 무적 시 로우패스 필터로 몰입.
- **모든 음원은 라이선스 확인된 것만**(이전 논의 반영).

---

# ④ 메타 · 수익화 디테일 — ⚠️ 대부분 폐기·대체됨 (과거 기록)

> **현행 사양은 [바운스타워_제품화설계.md](바운스타워_제품화설계.md)**. 아래와의 차이:
>
> | 이 문서 | 실제 |
> |---|---|
> | 4.1 코인 + 젬 2화폐 | **코인 하나** (젬 폐기) |
> | 4.4 IAP 카탈로그(광고제거·스타터팩·젬·배틀패스) | **전부 미도입** — 광고 전용 |
> | 4.5 미션 "젬 20개" | 코인 기준 미션으로 구현 |
> | 4.3 전면광고 3~4판마다 + 첫 세션 3판 금지 | ✅ 구현 — 첫 3판 무광고 + 3판마다 1회(둘 다 Remote Config로 조정), 45초 타이머는 백스톱 |
>
> 4.2(스킨=외형만, 시즌한정 FOMO)와 4.5 출석 7일 사이클은 **채택·구현됨**.

## 4.1 화폐 구조
| 화폐 | 획득 | 용도 |
|------|------|------|
| 코인(소프트) | 플레이·미션·광고 | 스킨 해금, 소모품 |
| 젬(하드) | 결제·소량 보상 | 프리미엄 스킨, 코인 환전 |

## 4.2 스킨 시스템 (주 수익·리텐션 동력)
- **공 스킨**: 외형 + 고유 트레일/파티클(차별 포인트).
- **타워 테마**: 기둥·링·배경 세트 변경.
- **희귀도**: 일반/레어/에픽/시즌한정. 한정 = FOMO.
- 해금: 코인 / 젬 / 배틀패스 / 이벤트 보상.
- ⚖️ 스킨은 **외형만** — 성능 차 금지(Pay-to-Win 금지).

## 4.3 광고 배치 (수익 vs 이탈 균형)
| 위치 | 유형 | 빈도 |
|------|------|------|
| 게임오버 후 부활 | 보상형 | 1판 1회 |
| 보상 2배 받기 | 보상형 | 데일리/판 종료 |
| 코인 부스터 | 보상형 | 상점/판 종료 |
| 판 사이 전면 | 전면 | **3~4판마다 1회**(타이머 가드) |
- **첫 세션 3판은 전면 광고 금지** → 첫인상 보호(D1 리텐션 직결).
- 광고제거 IAP 구매자는 전면 0(보상형은 선택 유지).

## 4.4 IAP 카탈로그
- 광고 제거 (₩3,300~5,500) — 캐주얼 핵심 결제, 눈에 띄게 배치.
- 스타터 팩(코인+젬+스킨, 첫 구매 한정 할인).
- 젬 패키지(소/중/대).
- 시즌 배틀패스(무료+유료 트랙).

## 4.5 데일리 & 미션 (리텐션 엔진)
- **출석 보상**: 7일 사이클, 연속 보너스.
- **데일리 미션 3종**: "30콤보 달성", "50링 도달", "젬 20개" → 코인 보상.
- **데일리 런 랭킹**: 친구 비교 → 사회적 복귀 동기.

## 4.6 LiveOps (출시 후 운영)
- 주간 이벤트(테마 타워, 한정 스킨).
- 시즌(배틀패스 12주 주기).
- 푸시 알림(이탈 D3·D7 복귀 유도, 과하지 않게).
- A/B 테스트: 광고 빈도·난이도 곡선·가격 → Remote Config로 무배포 튜닝.

## 4.7 측정해야 할 지표
| 지표 | 의미 |
|------|------|
| D1/D7/D30 리텐션 | 게임 건강도 |
| 평균 세션·세션수 | 몰입도 |
| ARPDAU | 유저당 일매출 |
| 광고 노출/유저, eCPM | 광고 수익 효율 |
| 부활 광고 시청률 | 핵심 광고 포인트 작동 여부 |
| CPI vs LTV | **출시 확대 판단의 최종 지표** |

---

# 우선순위 제안 (구현 순서)

1. **① 레벨 생성 + ② 콤보/무적/점수** → 코어 재미 완성 (Phase 0~1).
2. **③ 주스 연출 + 아이덴티티** → 손맛·차별화 (Phase 1).
3. **④ 메타·수익화** → 리텐션·매출 (Phase 2).
4. 소프트런치 → CPI/LTV 측정 → 글로벌 (Phase 3~4).

---

*문서 버전: v0.1 / 작성일: 2026-06-04*
