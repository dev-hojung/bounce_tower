# ⚪ 바운스 타워 — 메커닉 A 상세 설계 & Phase 0 프로토타입 가이드 v0.1

> 메커닉 A(나선 타워 낙하, Helix Jump 류) 확정 후 상세 설계.
> 목표: **이 문서만 보고 Phase 0 프로토타입을 바로 착수**할 수 있게 한다.

> ⚠️ **문서 상태 (2026-08-14)**: Phase 0 착수용 원본 설계서로, 코어 메커닉 서술은
> 지금도 유효하다. 다만 **이미 구현이 끝난 단계**이며 수익화·메타 관련 서술(젬 등)은
> 폐기됐다. 현행 사양은 [바운스타워_제품화설계.md](바운스타워_제품화설계.md),
> 코드 구조는 `bounce_tower/README.md`를 볼 것.

---

## 1. 게임 디자인 상세

### 1.1 무대 구조
- 중앙에 **수직 기둥(central pole)**. 그 둘레로 **플랫폼 링(ring)**들이 일정 간격으로 쌓여 있다.
- 각 링은 여러 **부채꼴 조각(segment)**으로 구성:
  - **안전 조각** (밟으면 튕김)
  - **빈 틈** (공이 통과하는 통로)
  - **위험 조각** (닿으면 게임오버 — 보통 빨강/가시)
- 카메라는 공을 따라 **아래로 스크롤**. 화면엔 항상 2~3개 링이 보인다.

### 1.2 공(ball)의 거동
- 공은 중앙 기둥 근처에서 **중력으로 낙하 → 안전 조각에 닿으면 위로 튕김(bounce)**.
- 같은 높이에서 통통 튀다가, 플레이어가 **틈을 맞춰주면 아래 링으로 통과**.
- 핵심 쾌감: **여러 링을 연속 통과(콤보)** 하면 공이 잠깐 무적이 되어 위험 조각까지 부수며 내려감 → 화면 폭발.

### 1.3 조작
- **드래그(좌우 스와이프)** = 타워 전체 회전 (공은 X축 고정, 타워가 돈다).
- 공의 낙하/튕김은 자동. 플레이어는 **회전으로 틈 정렬**만 한다.
- → 한 손 엄지로 완결. 언어 무관.

### 1.4 게임오버 / 점수
- **게임오버**: 공이 위험 조각에 (무적 아닐 때) 닿음.
- **점수**: 내려간 깊이(통과한 링 수) 기반. 콤보 통과 시 배율 보너스.
- **콤보**: 안전 조각을 밟지 않고 N개 링 연속 통과 → 무적 + 배율↑ + 이펙트.

### 1.5 난이도 곡선 (가속 설계)
| 깊이 | 변화 |
|------|------|
| 0~10링 | 틈 넓음, 위험 조각 적음, 천천히 (온보딩) |
| 10~30링 | 틈 좁아짐, 위험 조각 비율↑ |
| 30+링 | 위험 조각 패턴 복잡, 회전 정밀도 요구 |
- 낙하 속도는 살짝만 증가(과하면 조작 불능). **난이도는 주로 레이아웃으로** 조절.

### 1.6 특수 요소 (Phase 1 이후)
- 보너스 젬(코인), 점수 부스터 조각, 색 테마 변화 구간, 보스 링 등.

---

## 2. Phase 0 프로토타입 범위 (1~2주)

> **검증 질문: "통통 튕기며 틈으로 떨어뜨리는 게 30초 해보면 '다시!'가 나오는가?"**

### 포함 (Must)
- [ ] 중앙 기둥 + 링 3~5개 생성 (안전/틈/위험 조각)
- [ ] 공 낙하·튕김 물리
- [ ] 드래그로 타워 회전
- [ ] 위험 조각 충돌 → 게임오버 → 재시작
- [ ] 통과한 링 수 = 점수 표시
- [ ] 무한 생성(공이 내려가면 위쪽 링 재활용 = 오브젝트 풀)

### 제외 (Phase 0에선 안 함)
- 콤보 무적, 특수조각, 스킨, 광고, 사운드 폴리싱, 메뉴 — **재미 검증에 불필요**.

### 성공 기준
- 본인/지인 5명에게 30초씩 → "한 판 더" 자연 발생 ≥ 3명 → 통과. 아니면 파라미터 튜닝 or 피벗.

---

## 3. Unity 기술 설계

### 3.1 기본 선택
- **2D vs 3D**: Helix Jump는 3D(원통 회전)지만, **프로토타입은 3D가 오히려 직관적**. Unity 3D + 내장 물리(Rigidbody/Collider) 권장.
- **고정 타임스텝**: `Project Settings > Time > Fixed Timestep = 0.0166`(60Hz). 물리 일관성 핵심.

### 3.2 씬 계층 구조
```
- Main Camera (공의 Y를 따라가는 Follow 스크립트)
- Directional Light
- CentralPole (정적 실린더)
- TowerRoot (회전하는 부모 오브젝트)   ← 드래그로 이 오브젝트만 Y축 회전
    └ Ring_0 ... Ring_N (각 링)
         └ Segment_x (부채꼴 메시 + Collider, 태그: Safe / Danger / 빈틈은 없음)
- Ball (Rigidbody + SphereCollider, 물리로 튕김)
- GameManager (점수·상태·생성 관리)
- UI Canvas (점수 텍스트, 게임오버 패널)
```

### 3.3 핵심 컴포넌트

**Ball (공)**
- `Rigidbody`: useGravity = true, 회전 고정(X·Z), Interpolate = Interpolate.
- 튕김은 **물리 머티리얼(Physic Material)**의 Bounciness로 처리하거나, 충돌 시 코드로 `velocity.y = bounceForce` 직접 세팅(더 일관적·추천).
- X·Z 위치 고정 → 공은 제자리에서 위아래로만, **회전은 타워가** 담당.

```csharp
// BallController.cs (핵심 발췌 — 의사코드 수준)
void OnCollisionEnter(Collision col) {
    if (col.gameObject.CompareTag("Danger") && !isInvincible) {
        GameManager.Instance.GameOver();
    } else if (col.gameObject.CompareTag("Safe")) {
        // 일관된 튕김: 속도를 직접 세팅
        rb.velocity = new Vector3(0, bounceForce, 0);
    }
}
```

**TowerRotator (회전 입력)**
```csharp
// 드래그 델타 → 타워 Y축 회전
void Update() {
    if (Input.GetMouseButton(0)) {
        float dx = Input.GetAxis("Mouse X"); // 모바일은 touch.deltaPosition.x
        transform.Rotate(0, -dx * rotateSpeed, 0);
    }
}
```

**Ring / Segment 생성**
- 한 링 = N개 세그먼트 슬롯. 일부는 Safe, 일부는 Danger, **최소 1개는 빈 틈(콜라이더 없음)**.
- 난이도에 따라 Danger 비율·틈 개수 조절.
- **오브젝트 풀링**: 공이 충분히 내려가면 화면 위로 사라진 링을 아래에 재배치(무한 타워 착시) → 메모리 일정.

**GameManager**
- 상태: Ready / Playing / GameOver.
- 통과 감지: 공이 한 링의 Y를 지나면 `score++`, 콤보 카운트.
- 재시작: 씬 리로드 또는 상태 리셋.

### 3.4 튜닝해야 할 물리 파라미터 (Phase 0 핵심 작업)
| 파라미터 | 시작값(감) | 설명 |
|----------|-----------|------|
| gravity | -25 ~ -40 | 기본 -9.81은 너무 둥실거림. 키워야 손맛 |
| bounceForce | 8 ~ 12 | 안전 조각 튕김 높이 |
| rotateSpeed | 0.2 ~ 0.4 | 드래그 민감도 (너무 빠르면 멀미) |
| ring 간격 | 1.5 ~ 2.5 | 링 사이 수직 거리 |
| Danger 비율 | 0.1 → 0.4 | 깊이에 따라 증가 |

> ⚠️ 이 5개 값의 조합이 **재미의 90%**. Phase 0는 사실상 이 튜닝 작업.

---

## 4. 흔한 함정 (미리 피하기)

- **공을 회전시키지 말 것**: 공은 고정, **타워를 회전**. 카메라 멀미·조작 혼란 방지.
- **물리 머티리얼 Bounciness만 의존 금지**: 기기·프레임에 따라 튕김 높이가 들쭉날쭉. **코드로 velocity 세팅**이 일관적.
- **Update에서 물리 조작 금지**: 충돌/속도는 `FixedUpdate`/충돌 콜백에서.
- **무한 생성 누락**: 풀링 없이 링을 계속 Instantiate하면 메모리 폭증.
- **난이도를 속도로만 올리기**: 낙하 속도↑는 금방 조작 불능. **레이아웃(틈·위험 비율)으로** 올릴 것.

---

## 5. Phase 0 → 1 체크포인트

프로토타입이 "다시!"를 끌어내면 다음으로:
1. **콤보 무적 + 파괴 연출** (이게 바이럴 핵심 쾌감)
2. 사운드·햅틱·파티클 (손맛 폴리싱)
3. 비주얼 아이덴티티 확정 (레드오션 차별화)
4. 메타·수익화 (스킨·광고·랭킹)

---

## 6. 권장 다음 액션

- [ ] Unity 3D 프로젝트 생성, 위 씬 구조 셋업
- [ ] BallController / TowerRotator / GameManager 최소 구현
- [ ] 물리 5개 파라미터 튜닝 → 손맛 잡기
- [ ] 지인 테스트 → 통과/피벗 판단

---

*문서 버전: v0.1 / 작성일: 2026-06-04 / 상위 문서: 물리아케이드_기획서.md*
