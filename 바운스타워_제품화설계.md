# 바운스타워 — 제품화 설계서

> 대상 영역: **① 상점·경제 확장 / ② 리텐션 시스템 / ③ 출시 필수 항목**
> 기준: Flutter WebView(게임=Three.js `assets/www`) + 네이티브 AdMob 구조.
> 작성일 2026-06-23. 관련: [바운스타워_INDEX](바운스타워_INDEX.md), `bounce_tower/README.md`

---

## 0. 구현 현황

### 0-1. 설계 기준선 (2026-06-23 — 이 문서를 쓰게 만든 상태, 역사 기록)

| 시스템 | 당시 상태 |
|---|---|
| 코어 | 무한 하강, 콤보, 무적(5콤보→7링 관통), 목숨 3 — 완성 |
| 코인 | 획득·소비 동작 |
| 스킨 | 공 스킨 7종 (50~600코인) |
| 데일리 | 출석 보상(단발), 연속 보너스 없음 |
| 미션 | 3종(콤보/깊이/코인), 일일만 |
| 랭킹 | **가짜 데이터** |
| 광고 | 부활·코인2배 (AdMob 골격 완료, 테스트 ID) |
| IAP | 광고제거·젬 **버튼만(목업)** |
| 세이브 | localStorage, 클라우드 없음 |

당시 진단: **재화는 코인 하나뿐, 수집은 공 스킨뿐, 리텐션 장치는 약함.**

### 0-2. 현재 (2026-08-14)

| 시스템 | 상태 |
|---|---|
| 코어 | 동일. 단 `dangerMax`·`rageCombo`가 **Remote Config로 조정 가능** |
| 코인 | `max(2, floor(depth*0.7)) + 코인발판` × 광고2배 × 부스터 × `coin_mult` |
| 수집 | 공 스킨 7 + **트레일 5 + 배경테마 5 + 소모품 3 + 시즌 한정 스킨 3** |
| 데일리 | **7일 사이클 + 연속 스트릭** (끊기면 리셋, 7일차 300🪙) |
| 미션 | **일일 3 + 주간 3 + 업적 6**(평생 1회) |
| 랭킹 | **Firebase 실연동** — 전체(명예의 전당) + **2주 시즌** 2탭 |
| 광고 | 동일(테스트 ID). 전면 빈도 **원격 조정 가능** |
| IAP | **폐기** — 광고 전용 수익 모델 확정, 목업 결제 버튼 제거 |
| 세이브 | localStorage + **클라우드 백업**(`users/{uid}.save`, 익명 uid 기준) |
| 알림 | **로컬 알림 3종** — 매일 출석/2일 복귀/주간 마감 |

남은 큰 구멍: **분석(Analytics·Crashlytics) 없음**, **재설치 복원 불가**(소셜 로그인 미연결).

---

## 1. 상점·경제 확장

### 1.1 2재화 체계 — ❌ **폐기 (미채택)**

> 수익 모델을 **광고 전용**으로 확정하면서 젬(하드 커런시)과 IAP를 통째로 뺐다.
> 상점의 목업 결제 버튼도 제거. 재화는 🪙 코인 하나뿐이며, 젬을 전제로 한 아래
> 설계(환전·프리미엄 스킨·젬 보상)는 **참고용 과거 기록**이다.
> 코드에 `setAdFree` 브리지와 `AdManager.adFree`가 dormant로 남아 있어, 향후
> "광고 제거" IAP만 도입하려면 그 지점부터 되살리면 된다.

| 재화 | 성격 | 획득 | 용도 |
|---|---|---|---|
| 🪙 **코인** (소프트) | 대량 순환 | 플레이(런당 평균 후술), 미션, 출석, 보상형광고 | 일반 스킨/트레일/소모품 |
| 💎 **젬** (하드) | 희소 | IAP, 업적/마일스톤 보상(소량), 출석 7일차 | 프리미엄 스킨, 컨티뉴, 코인 환전, 광고제거 |

- 환전 가이드: **1💎 ≈ 100🪙** 체감. 젬→코인 환전만 허용(역방향 불가).
- 젬은 "아껴 쓰는 프리미엄"으로 포지셔닝 → 과금 가치 보호.

### 1.2 상점 카테고리 확장 (수집 볼륨 ↑)
현재 1탭(공 스킨)을 **4 카테고리**로:

**A. 공 스킨** (기존 7 유지 + 확장)
| 등급 | 예시 | 가격 |
|---|---|---|
| 기본 | 클래식 | 0 |
| 일반 | 아쿠아/라임 | 50~90🪙 |
| 고급 | 골드/바이올렛 | 160🪙 |
| 희귀 | 섀도우 | 300🪙 |
| 프리미엄 | 레인보우(애니), 신규 갤럭시/네온 | 600🪙 또는 💎 |

**B. 트레일 이펙트** (신규) — 공 뒤 잔상 색/모양
- 기본 라인 / 별가루 / 불꽃 / 네온 리본 / 무지개. 80~250🪙.
- 구현: 게임의 TrailRenderer 색·텍스처·길이를 스킨처럼 데이터화.

**C. 배경 테마** (신규) — 오로라 그라데이션 색 조합
- 오로라(기본)/석양/심해/사이버펑크/벚꽃. 120~300🪙.
- 구현: 하늘 셰이더 `cTop/cMid/cBot` + 조명색 프리셋 교체.

**D. 사망/통과 이펙트** (선택, 후순위)
- burst 파티클 색·모양 변형.

> 효과: 상점 아이템 7개 → **약 25~30개**. 코인 소비처 확보 = 플레이 동기 + 광고 시청 동기.

### 1.3 코인 밸런스 / 언락 곡선
- 런당 평균 코인: 초반 사망 기준 깊이~15, 점수~20 → **약 17🪙/런**. 숙련 시 40~60🪙.
- 일 평균 플레이 10런 가정 → **170~400🪙/일**.
- 목표 언락 속도: 첫 스킨(50) 1일차, 고급(160) 2~3일차, 600짜리 1주차. → 가격대 현행 유지 OK.
- **무료 코인 수도꼭지**: 보상형광고(코인2배), 미션, 출석. 인플레 방지 위해 아이템 추가로 흡수.

### 1.4 소모품(컨슈머블) — ✅ 구현 (코인 전용, 젬 결제 옵션은 폐기)
| 아이템 | 효과 | 가격 |
|---|---|---|
| 컨티뉴/부활권 | 사망 시 즉시 1회 부활(광고 대체) | 80🪙 |
| 코인 부스터(×2, 3런) | 다음 3런 코인 2배 | 120🪙 |
| 시작 보호막 | 첫 피격 1회 무효 | 60🪙 |

→ 보상형광고(무료 부활)와 공존: 광고 보기 싫은 유저는 구매.

### 1.5 상점 IA/UX — ✅ 구현
- ✅ 상단 탭: 공 / 트레일 / 배경 / 소모품 (`.shoptab`, `shopCat`)
- ✅ 코인 표시 상단 고정 — ❌ 젬·"충전"(IAP) 버튼은 폐기로 미도입
- ✅ 장착 시 홈 배경의 공/타워에 즉시 반영(`setRage(false)` / `applyTheme()`)
- ⬜ 미도입: **구매 전 미리보기**(현재는 구매·장착해야 반영됨)

---

## 2. 리텐션 시스템

### 2.1 출석(데일리) — 7일 사이클 + 연속 보너스
현재 단발 → **7일 곡선**으로:
| 일차 | 1 | 2 | 3 | 4 | 5 | 6 | 7 |
|---|---|---|---|---|---|---|---|
| 보상 | 30🪙 | 50🪙 | 80🪙 | 부스터 | 120🪙 | 트레일 1종 | **2💎 + 200🪙** |
- 연속 끊기면 1일차로 리셋(또는 1회 "연속 보호" 아이템). 7일 후 사이클 반복(보상 소폭 상향).

### 2.2 미션 재편 — 일일 / 주간 / 업적
- **일일**(현행 3종 유지, 매일 리셋): 콤보 N, 깊이 N, 코인 N → 각 35~50🪙
- **주간**(신규): "주 5회 플레이", "누적 깊이 500", "스킨 1개 구매" → 큰 보상(💎/희귀스킨 조각)
- **업적**(신규, 1회성 영구): "깊이 100 도달", "무적 10회", "스킨 10종 보유" → 💎·타이틀
- UI: 미션 탭에 일일/주간/업적 세그먼트

### 2.3 목숨/에너지 정책 — **도입 안 함(권고)**
- 캐주얼 무한 게임에 에너지 게이트는 세션 단절 → 리텐션 역효과.
- 대신 **광고 부활 + 컨티뉴 아이템**으로 세션 연장 유도. ("목숨 회복 알림"은 불필요.)

### 2.4 푸시 알림 시나리오 — ✅ 3종 모두 구현 (`lib/notifications.dart`)
| 트리거 | 시점 | 구현 |
|---|---|---|
| 데일리 리셋 | **매일 저녁 7시**(오전 아님) | id:1, 매일 반복 |
| 이탈 복귀 | 미접속 2일 | id:2, 앱 실행 때마다 리셋 |
| 주간 미션 마감 | 일요일 저녁 8시 | id:3, 매주 반복 |
- FCM(원격 푸시)은 미도입 — 전부 **로컬 알림**이라 서버가 필요 없다.
- ✅ 알림 권한은 설계대로 **첫 보상 수령 직후**(출석/미션 클레임)에 한 번만 요청한다
  (`askNotifPermissionOnce` → 브리지 `requestNotificationPermission`). 앱 시작 시
  ATT와 팝업이 겹치지 않는다. 예약 자체는 권한과 무관하게 시작 시 수행하므로
  2일 복귀 타이머 리셋은 그대로 동작한다.

### 2.5 온보딩 / 게임오버 훅
- **첫 플레이 튜토리얼**: 현재 "게임 방법" 카드 + 첫 런에서 손가락 가이드 1회(드래그 화살표).
- **게임오버 화면 리텐션 훅**: 현재(점수·부활·코인2배·다시/홈) 유지 + "다음 목표까지 깊이 N" 진행바 노출 → 재도전 동기.

### 2.6 시즌/이벤트 — ✅ 구현 (배틀패스는 여전히 보류)
- **2주 시즌 랭킹**: `2026-01-05 00:00 UTC` 기준 14일 주기. 시즌 점수는
  `seasons/{seasonId}/scores/{uid}`에 따로 쌓이고 전체 랭킹은 명예의 전당으로 유지.
- **시즌 한정 스킨** 3종 로테이션(네뷸라/엠버/프로스트): 시즌 최고점이 `season_goal`
  (기본 60점) 이상이면 해금. 코인으로 구매 불가, 시즌 종료 후 획득 경로 소멸.
  이미 획득한 스킨은 영구 보유·장착 가능하며 상점에 계속 노출된다.
- `season_enabled=false`로 원격에서 시즌 전체를 끌 수 있다(탭·해금 동시 차단).
- 배틀패스는 규모 커지면 검토(초기 비권장) — 여전히 미도입.

---

## 3. 출시 필수 항목 (게임성 외 — 없으면 스토어 통과/운영 불가)

### 3.1 스토어 자산
- [ ] 앱 아이콘(1024²), 적응형 아이콘(Android)
- [ ] 스플래시(브랜드 색 #241638 + 로고)
- [ ] 앱 이름 최종(현 "Bounce Tower") / 부제 / 키워드(ASO)
- [ ] 스크린샷 6.7"/6.5"/iPad, 프로모 영상(선택)
- [ ] 스토어 설명문(한/영)

### 3.2 법적·정책
- [ ] **개인정보처리방침 URL** (AdMob·분석 사용 → 필수). 정적 페이지로 Railway/깃허브 페이지 호스팅 가능
- [ ] **iOS ATT**(App Tracking Transparency) 동의 프롬프트 — 광고 ID 사용
- [ ] GDPR/UMP 동의(유럽 타겟 시) — Google UMP SDK
- [ ] 아동 대상 여부(COPPA) 체크 → 광고 콘텐츠 등급 설정
- [ ] 결제/환불 약관(IAP 도입 시)

### 3.3 분석 (Firebase Analytics + Crashlytics) — ✅ 구현 (`lib/analytics.dart`)

구현된 이벤트 9종 — 웹이 `logEvent` 브리지로 보내고 Dart가 이름·파라미터를 검증한다.
`iap_purchase`는 IAP 폐기로 **제외**, 시즌용 `season_skin_unlock`을 **추가**했다.

| 이벤트 | 파라미터 |
|---|---|
| `game_start` / `game_over` | games / score, depth, combo_max |
| `revive` | source(`ad`\|`item`) |
| `coin_double` | source, coins |
| `shop_buy` | item, kind(skin/trail/theme/item), price |
| `mission_claim` | type(daily/weekly/achv), id |
| `daily_claim` | day(연속 일수) |
| `season_skin_unlock` | season, skin, score |

- Crashlytics는 `FlutterError.onError` + `PlatformDispatcher.onError`를 잡는다.
  **Firebase 초기화를 `runApp` 이전으로 옮겨서** 초기 프레임 예외까지 수집된다.
- ⬜ 미구현: `ad_impression` / `ad_reward` — AdMob 실 ID 교체 시 함께 붙이는 게 좋다.

아래는 원래 설계안(참고):
| 이벤트 | 파라미터 | 목적 |
|---|---|---|
| `game_start` / `game_over` | score, depth, combo_max | 난이도·세션 길이 |
| `revive` / `coin_double` | source(ad/item) | 광고 가치 |
| `ad_impression` / `ad_reward` | type, placement | ARPU, fill |
| `iap_purchase` | product_id, price | 매출 |
| `shop_buy` | item, currency | 경제 흐름 |
| `mission_claim`, `daily_claim` | day, type | 리텐션 |
- 퍼널: 설치→첫런→D1→첫구매. KPI: **D1/D7 리텐션, ARPDAU, 광고시청률**.

### 3.4 원격 설정 (Firebase Remote Config) — ✅ 구현 (`lib/remote_config.dart`)
실제 키(설계안의 가칭과 이름이 다름):

| 키 | 기본 | 범위 | 대상 |
|---|---|---|---|
| `danger_max` | 0.42 | 0~0.8 | 밸런스 — 최대 위험 발판 비율 |
| `rage_combo` | 5 | 2~30 | 밸런스 — 무적 발동 콤보 |
| `coin_mult` | 1.0 | 0.5~10 | 이벤트 — 코인 획득 배율 |
| `first_runs_no_ads` | 3 | 0~50 | 광고 — 첫 N판 무광고(첫인상 보호) |
| `interstitial_every_runs` | 3 | 1~20 | 광고 — M판마다 전면 1회 |
| `interstitial_gap_sec` | 45 | 15~600 | 광고 — 전면 최소 간격(백스톱) |
| `season_enabled` | true | — | 이벤트 토글 — 시즌 on/off |
| `season_goal` | 60 | 10~100000 | 시즌 한정 스킨 해금 점수 |

- 원격 값은 코드가 **안전 범위로 클램프**하므로 콘솔 오입력이 게임을 깨지 않는다.
- 미도입: **가격표 원격화**(스킨/소모품 가격은 아직 웹 상수), 배너 광고(배너 자체가 없음).
- 콘솔 등록 절차: `FIREBASE_SETUP.md` 5장. 등록 안 해도 기본값으로 동작.

### 3.5 크래시·성능
- [ ] Firebase Crashlytics
- [ ] WebView 성능 모니터(저사양 기기 FPS), 메모리

### 3.6 빌드/배포 체크리스트
- [ ] AdMob 실 ID 교체(3곳: `ads.dart`, AndroidManifest, Info.plist)
- [ ] 번들 ID / 서명(iOS 인증서, Android 키스토어)
- [ ] 버전·빌드넘버 정책
- [ ] TestFlight / 내부테스트 트랙 → 단계적 출시

---

## 4. 우선순위 로드맵 (2026-08-14 기준)

**스프린트 1 — 출시 가능 최소선(MLP)** · 🟡 부분
- ✅ ATT, 개인정보처리방침, 아이콘/스플래시/앱 이름
- ⬜ **AdMob 실 ID 교체**(3곳 전부 테스트 ID — 수익 직결, 최우선)
- ⬜ iOS `SKAdNetworkItems`
- ✅ Firebase Analytics + Crashlytics (이벤트 9종, §3.3)
- ⬜ 스토어 자산·서명·제출

**스프린트 2 — 경제·수집 깊이** · ✅ 완료 (젬/IAP는 폐기로 종결)
- ✅ 트레일 5종 / 배경 테마 5종 / 소모품 3종 / 상점 4탭 IA 개편
- ❌ 젬 재화 + IAP 실제 결제 → 광고 전용 모델로 **미채택**(§1.1)

**스프린트 3 — 리텐션** · ✅ 완료
- ✅ 7일 출석 곡선 + 연속 스트릭, 주간/업적 미션, 로컬 푸시 3종
- ✅ 실제 랭킹 — Play Games/Game Center 대신 **Firebase**로 구현
  (크로스플랫폼 단일 랭킹 + 닉네임 자유도 확보가 이유)

**스프린트 4 — 라이브옵스** · ✅ 완료
- ✅ 2주 시즌 랭킹 + 시즌 한정 스킨 3종 로테이션
- ✅ Remote Config (밸런스·광고빈도·이벤트 배율·시즌 토글)
- ⬜ 남은 것: 가격표 원격화, 배틀패스(보류)

**다음** — 출시 후 1차 업데이트
- ⬜ 소셜 로그인 연결(Google/Apple) → 재설치·기기이전 복원. `FIREBASE_SETUP.md` 2-B

---

## 부록: 세이브 스키마 (실제 — `bt_save_v2`, `load()` 기준)

```js
SAVE = {
  coins, best,                                   // 재화는 코인 하나 (젬 폐기)
  ownedSkins[], equipped,                        // 시즌 한정 스킨도 여기 함께 보관
  ownedTrails[], equippedTrail,
  ownedThemes[], equippedTheme,
  items: { revive, booster, shield },            // 소모품 보유 수
  boosterRuns,                                   // 코인 부스터 잔여 판수
  adFree,                                        // dormant (IAP 미도입)
  nickname, nickSet,                             // 닉네임 + 사용자 지정 여부
  daily:    { last, streak },                    // 7일 사이클 + 연속 일수
  missions: { date, defs[], prog{} },            // 일일 (날짜 바뀌면 리셋)
  weekly:   { week, defs[], prog{} },            // 주간 (주 바뀌면 리셋)
  achvClaimed: { [id]: true },                   // 업적 수령 여부 (영구)
  stats: { games, bestDepth, bestCombo, rages, coinsTotal },   // 업적 판정용 평생 통계
  season: { id, best },                          // 시즌 최고점 (시즌 바뀌면 리셋)
}
```

설계안과 달라진 점: `gems` 없음(폐기), `consumables` → `items` + `boosterRuns`,
`missions: {daily, weekly, achv}` 한 덩어리 대신 `missions`/`weekly`/`achvClaimed`로 분리
(리셋 주기가 달라 분리하는 편이 단순했다).

> ⚠️ localStorage는 앱 삭제/기기변경 시 소실 → **클라우드 세이브 구현 완료**
> (`users/{uid}.save`에 이 객체 통째 백업). 다만 익명 uid 기준이라 **재설치 복원은
> 아직 안 된다** — 소셜 로그인 연결(`FIREBASE_SETUP.md` 2-B) 필요.
