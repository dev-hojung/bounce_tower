# Bounce Tower — 출시 체크리스트 (스프린트 1: MLP)

스토어 제출 가능한 최소 제품(MLP) 기준. ✅ = 코드로 완료됨, ⬜ = **사용자 계정/작업 필요**.

---

> **수익 모델: 광고 전용.** 유료 상품(IAP) 없음 — 상점의 목업 결제 버튼(광고 제거/젬 충전)은 제거됨. 젬은 게임 내 무료 획득만.

## A. 완료됨 (코드 반영)

- ✅ **iOS ATT 동의** — `app_tracking_transparency` + `Info.plist`(NSUserTrackingUsageDescription) + 앱 활성화 후 권한 요청 (`lib/main.dart`)
- ✅ **앱 아이콘** — `assets/icon/icon.png` → `flutter_launcher_icons` 적용(Android/iOS)
- ✅ **스플래시** — `assets/icon/splash.png` + 배경색 `#241638` → `flutter_native_splash`
- ✅ **앱 이름** — "Bounce Tower" (iOS CFBundleDisplayName / Android label)
- ✅ **개인정보처리방침** — https://bounce-tower-web-production.up.railway.app/privacy
- ✅ **AdMob 연동 골격** — 전면/보상형 + 빈도제한 (현재 **테스트 ID**)
- ✅ **Firebase 랭킹·클라우드 세이브** — 익명 인증 + Firestore(`scores`/`seasons`/`users`) 구성 완료
- ✅ **닉네임 설정/변경 UI** — 웹 모달 + 검증 + 네이티브 재제출 (`assets/www/index.html`)
- ✅ **상점 확장** — 트레일 5 / 배경테마 5 / 소모품 3, 4탭 IA (총 20종+)
- ✅ **리텐션** — 7일 출석 사이클 + 스트릭, 일일/주간/업적 미션, 로컬 알림 3종
- ✅ **시즌 (2주)** — 시즌 랭킹 + 한정 스킨 3종 로테이션 (`seasons/{seasonId}/scores`)
- ✅ **Remote Config** — 밸런스·전면광고 빈도·코인 배율·시즌 토글 원격 조정
- ✅ **Analytics + Crashlytics** — 이벤트 9종 + 크래시 핸들러(runApp 이전 등록)
- ✅ **광고 빈도 정책** — 첫 3판 무광고 + 3판마다 1회(45초 타이머는 백스톱)
- ✅ **알림 권한 타이밍** — 앱 시작이 아닌 첫 보상 수령 직후 1회 요청
- ✅ **Firebase 백엔드 활성화** — 익명 인증 ON + 규칙 재배포로 랭킹·클라우드 세이브·시즌이 **실제 동작 시작**(2026-09-17)
- ✅ **계정 연결(Google/Apple) 코드** — 계정 모달·연결·전환 병합·로그아웃·**계정 삭제**(App Store 5.1.1(v)). 콘솔 설정 전에는 안전 실패

> ✅ `firestore.rules` 재배포 완료(2026-09-17). 이후 규칙을 고치면 다시 배포할 것.

---

## B. 사용자 작업 필요 (계정/콘솔)

### B-1. AdMob 실제 광고 단위 ID ⬜
> 현재 Google 테스트 ID. 실 ID로 바꿔야 수익 발생. **개발 중 실광고 클릭 금지(계정 정지 위험)**.

1. https://admob.google.com 에서 앱 2개 등록(iOS/Android)
2. 각 앱에 광고 단위 생성: **전면(Interstitial)**, **보상형(Rewarded)**
3. 발급받은 ID로 아래 3곳 교체:
   - `lib/ads.dart` → `_AdUnits.interstitial`, `_AdUnits.rewarded` (플랫폼별)
   - `android/app/src/main/AndroidManifest.xml` → `com.google.android.gms.ads.APPLICATION_ID`
   - `ios/Runner/Info.plist` → `GADApplicationIdentifier`

### B-1b. iOS SKAdNetwork 항목 ✅ 완료 (2026-09-17)
> `Info.plist`에 AdMob 문서 기준 **50개** 등록. 미디에이션 네트워크를 추가하면 그때 갱신할 것.

### B-2. Firebase Analytics + Crashlytics ✅ 코드 완료 — 콘솔 확인만 필요
> 패키지·이벤트 9종·크래시 핸들러 모두 붙었다(`lib/analytics.dart`, `제품화설계 3.3`).
> 남은 건 콘솔에서 데이터가 들어오는지 확인하는 것뿐:

1. Firebase 콘솔 → Analytics → DebugView 에서 `game_start`/`game_over` 수신 확인
2. Firebase 콘솔 → Crashlytics → 첫 크래시 리포트 수신 확인
   (테스트: `FirebaseCrashlytics.instance.crash()` 를 임시 버튼에 걸어 1회 발생시킨 뒤 제거)
3. 실 광고 ID 교체 시 `ad_impression` / `ad_reward` 이벤트를 함께 추가할 것

### B-3. 스토어 등록 자산 ⬜
- 스크린샷(6.7"/6.5" iPhone, iPad) — 시뮬레이터/실기기에서 캡처
- 스토어 설명문(한/영), 키워드(ASO)
- 개발자 계정: Apple Developer($99/년), Google Play($25 1회)
- 번들 ID 확정 + 서명(iOS 인증서, Android 키스토어 — **키스토어 백업 필수**)
  · Android 서명 설정은 💻 **코드로 완료**: `android/key.properties`가 있으면 그 키로 서명하고 없으면 debug로 폴백한다.
    `android/key.properties.example`을 복사해 값만 채우면 된다(키스토어 생성 명령도 그 파일에 적어 뒀다).

### B-4. 빌드/제출 ⬜
```bash
flutter build appbundle --release   # Android (Play 업로드용 .aab)
flutter build ipa                   # iOS (App Store Connect 업로드)
```
- 먼저 내부테스트(Android) / TestFlight(iOS)로 검증 후 단계적 출시

### B-5. 소셜 로그인 프로비저닝 🟡 대부분 완료 (2026-09-17)
> 상세 절차: `FIREBASE_SETUP.md` 2-B.

- ✅ Firebase Authentication **익명 ON**(그전까지 미설정이라 랭킹·클라우드 세이브가 전부 목업으로만 돌았다), Google·Apple provider ON, 익명 자동 삭제 OFF
- ✅ `firestore.rules` 재배포 — 시즌 규칙 + 본인 `delete` 반영. 라이브 프로브로 허용/차단 동작 검증 완료
- ✅ **debug** SHA-1/SHA-256 등록 → `flutterfire configure` 재실행 → `oauth_client`(web/android) · iOS `CLIENT_ID` 채워짐
- ✅ iOS `Info.plist` URL scheme = `REVERSED_CLIENT_ID`
- ⬜ **upload 키스토어 SHA-1** 등록(B-4에서 키 생성 후) + **Play App Signing SHA-1**(Play Console 등록 후).
  ⚠️ 이 둘이 빠지면 **프로덕션에서만 Google 로그인이 실패**한다
- ⬜ Apple Developer → App ID에 **Sign in with Apple** capability 켜기(entitlements 파일은 이미 있음)
- ⬜ 실기기 e2e: 연결 → 재설치 복원 → 두 기기 병합 → 계정 삭제
- ⬜ 스토어 제출 시: App Privacy / Data Safety에 **User ID** 신고, 개인정보처리방침에 로그인 식별자 항목 추가

---

## 진행 순서 제안
1. **B-1(AdMob 실 ID)** — 제일 먼저, 수익 직결
2. **B-2(Firebase)** — `flutterfire configure`만 해주면 코드는 내가 마무리
3. **B-3/B-4** — 개발자 계정 준비되면 빌드·제출
4. **B-5(소셜 로그인 프로비저닝)** — 출시 직전 또는 1차 업데이트. 코드는 끝났고 콘솔 작업만 남음

> `flutterfire configure`, `railway login` 같은 로그인은 터미널에서 `! 명령어`로 직접 실행해 주세요.
