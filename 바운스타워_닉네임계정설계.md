# 바운스타워 — 랭킹 닉네임 · 계정 설계

> 작성일: 2026-06-28 · 설계 검토 반영: 2026-09-02 · 대상: 랭킹 식별(닉네임)과 계정(기기 이전) 처리
> 관련 문서: `FIREBASE_SETUP.md`(랭킹 백엔드 런북), `README.md`(WebView 구조)

## 0. 현재 상태 진단 (설계 출발점 — 2026-06-28 시점 스냅샷)

> 아래 표는 **설계 당시의 문제 상황**을 기록한 것이다(현재 상태 아님).
> 근거는 라인번호 대신 심볼로 표기 — 코드가 움직여도 찾을 수 있도록.

| 영역 | 당시 현황 | 근거 심볼 |
|---|---|---|
| 닉네임 | `Player+4자리난수` 자동 생성, **변경 UI 없음** | `index.html` `load()` |
| 계정 | Firebase **익명 인증**, `scores/{uid}` 문서 | `ranking.dart` `RankingService.init` |
| 점수 제출 | 네이티브 브리지 `submitScore(best, nickname)` | `index.html` `submitScoreNative` |
| 서버 규칙 | `name`은 string·≤16자만 검증, best 단조증가 강제 | `firestore.rules` |

**2대 한계**
1. ~~닉네임을 사용자가 바꿀 수 없음 → 랭킹이 전부 `PlayerXXXX`라 식별 곤란~~
   → ✅ **Phase 1로 해소**
2. 재설치/기기교체 시 익명 uid가 새로 발급 → 기존 랭킹·진행도 유실(복구 수단 없음)
   → ⬜ **미해소.** 클라우드 세이브가 들어갔지만 uid에 묶여 있어 Phase 2 없이는 복원 불가

설계를 **2단계**로 나눈다. Phase 1은 백엔드 변경 없이 즉시 적용, Phase 2는 기기 이전 대응.

**확정 결정**
- 닉네임 전역 유니크 검사: **하지 않음**(중복 허용, uid로 식별). 캐주얼 게임 관행 + Firestore 유니크 강제 비용 회피.

---

## Phase 1 — 닉네임 설정/변경 (웹 단독, 백엔드 무변경)

### 목표
사용자가 닉네임을 직접 정하고 언제든 바꾼다. 목업·네이티브 양쪽 동작.

### 1-1. 진입 동선 (UX)
- **최초 1회 권유**: 첫 실행 시 홈 진입 직후 닉네임 모달 노출 (`SAVE.nickSet`이 false일 때). "건너뛰기" 시 기존 `PlayerXXXX` 유지.
- **상시 변경**: 홈 상단바(`#home .top`)에 닉네임 칩 추가 → 탭 시 모달. 랭킹 화면 `tierBox`의 "내 최고점" 옆 ✏️ 보조 진입점.

```
홈 상단바:  [ 🪙 0 ]                       [ 👤 Player7421 ✏️ ]
                                                  └ 탭 → 닉네임 모달
┌────────── 닉네임 설정 ──────────┐
│  [ Player7421              ]  16/16 │  ← 실시간 글자수
│  한글/영문/숫자 2~12자             │
│  ⚠ 사용할 수 없는 문자입니다       │  ← 검증 실패 시 표시
│     [ 저장 ]      [ 취소 ]         │
└─────────────────────────────────┘
```

### 1-2. 검증 규칙 (클라이언트)
| 항목 | 규칙 |
|---|---|
| 길이 | 2~12자 (서버 16자 한도 내 여유) |
| 허용 문자 | 한글/영문/숫자/`_`/공백 — `^[가-힣A-Za-z0-9_ ]{2,12}$` |
| 트림 | 앞뒤 공백 제거, 연속 공백은 1개로 축소 |
| 금칙어 | 소형 비속어 블랙리스트(웹 상수)로 부분일치 차단 |
| 실패 처리 | 저장 거부 + 토스트 안내, 모달 유지 |

> 전역 중복 검사는 범위 외(중복 허용). 동일 닉이라도 uid로 구분되며 랭킹 행에는 닉만 노출.

### 1-3. 데이터 / 저장
- `SAVE.nickname` 그대로 사용. `load()`의 자동생성(`index.html:429`)은 "미설정 기본값"으로 유지.
- 새 플래그 `SAVE.nickSet`(bool): 사용자가 한 번이라도 저장하면 true → 최초 모달 재노출 방지.
- 저장 흐름:
  1. `SAVE.nickname = 정제값; SAVE.nickSet = true; save();`
  2. 홈/랭킹 닉 칩 갱신(`refreshMeta()`에 닉 렌더 추가)
  3. **네이티브면 즉시 재제출** → 기존 `submitScoreNative()` 재호출(`scores/{uid}.name` 갱신)

### 1-4. 손대는 곳 (웹 `assets/www/index.html` 만)
- HTML: `#home .top`에 닉 칩, 닉 설정 모달 1개 추가(`#admodal` 스타일 패턴 재사용)
- JS: `validateNick()` 검증 함수, 모달 open/save 핸들러, `refreshMeta()` 닉 표시, 첫 실행 모달 트리거
- **Dart/규칙 변경 없음** — `submitScore` 브리지·`firestore.rules`(name≤16) 이미 호환
- ⚠️ 원본 위치 정정: 이 문서 작성 당시엔 `bounce-tower-prototype/index.html`이 원본이었으나,
  **지금은 `assets/www/index.html`이 원본**이다(프로토타입은 2026-06-25에서 멈춤).
  프로토타입에서 복사해 오면 닉네임·클라우드세이브·시즌이 사라진다.

### 1-5. 작업 체크리스트 (Phase 1) — ✅ 완료
- [x] `SAVE.nickSet` 스키마 추가 + `load()` 기본값
- [x] 홈 상단 닉 칩 + 모달 HTML/CSS
- [x] `validateNick()` (정규식 + 트림 + 금칙어 19개)
- [x] 저장 핸들러 → save + UI 갱신 + 네이티브 재제출(`submitScoreNative`)
- [x] 첫 실행 시 모달 노출 트리거 + 건너뛰기(`nickSkip`도 `nickSet=true`로 기록)
- [x] 브라우저(목업)·네이티브 양쪽 동작 확인

> 추가로 구현된 것(설계 당시 미기재): 홈 닉 칩이 클라우드 활성 시 `👤`→`☁️`로 바뀌어
> 계정 동기화 상태를 겸해 보여준다.

---

## Phase 2 — 크로스 디바이스 계정 (기기 이전·재설치 대응)

> 2026-09-02 설계 검토 반영. 원안(2026-06-28)에서 바뀐 항목은 **[변경]**, 새로 추가된 절은 **[신규]**로 표시.
> 콘솔·플랫폼 프로비저닝 절차와 코드 착수 순서는 `FIREBASE_SETUP.md` 2-B.

### 목표
재설치/기기교체 후에도 같은 랭킹 기록·진행도를 잇는다. 익명 계정을 소셜 계정에 **연결(link)**.

### 2-1. 방식: 익명 → 영구계정 승급 (linkWithCredential)
- 익명 로그인은 유지(가입 마찰 0). 사용자가 원할 때만 "계정 연결".
- `currentUser.linkWithCredential(...)` / `linkWithProvider(...)` → 기존 uid·`scores`·`users` **그대로 승계**.
- 연결된 계정으로 다른 기기에서 로그인 시 동일 uid 복원 → 랭킹·세이브 복구.

**[변경] 제공 provider — 양 플랫폼 모두 Google + Apple**

| 플랫폼 | Google | Apple |
|---|---|---|
| iOS | ✅ `google_sign_in` | ✅ `AppleAuthProvider` (firebase_auth 내장, 네이티브) |
| Android | ✅ `google_sign_in` | ⬜ 2차 — `AppleAuthProvider` 웹 플로우(Services ID 등록 필요) |

- 원안(Android=Google, iOS=Apple)은 **iPhone→Android 기기 이전을 복원할 수 없다**. 기기 이전 복원이
  이 기능의 존재 이유이므로 양쪽에 둘 다 둔다.
- iOS에서 Google을 제공하면 App Store 4.8에 따라 Apple 로그인이 필수 → 어차피 둘 다 들어간다.
- **한 계정에 두 provider 모두 link 허용**(비익명 사용자에게 `linkWithCredential` 반복). 어느 경로로도 복원되게.

**[변경] `credential-already-in-use` 처리 — 고아 문서 정리 후 전환**

이미 다른 uid(B)에 연결된 자격증명으로 link하면 실패한다. 이때 현재 익명 uid(A)의 문서를 그대로 두고
B로 전환하면 랭킹에 `PlayerXXXX` 유령 행이 남는다. 순서:

1. **A로 로그인된 상태에서** A의 `scores/A`, `seasons/{현재}/scores/A`, `users/A` 삭제
   (규칙에 본인 `delete` 허용 추가 필요 — 현재 없음)
2. `signInWithCredential(B)`로 전환
3. 웹 `cloudResync()` — B의 클라우드를 받아 로컬과 병합 → 저장 → 점수 재제출 → 배지 갱신
4. 토스트 "다른 기기의 기록과 합쳐졌어요". **사용자에게 묻지 않는다** — 병합이 max/union이라 무손실.

1~2 사이에 실패해도 로컬(진실원본)이 살아 있으므로 다음 푸시가 A 문서를 다시 만든다. 데이터 손실 없음.

### 2-1a. [신규] 그대로 구현하면 깨지는 지점 — 연결 기능보다 먼저 고칠 것

| # | 문제 | 수정 |
|---|---|---|
| 1 | `RankingService.init()`이 매 실행 `signInAnonymously()`를 무조건 호출. Firebase SDK 문서: "다른 사용자가 로그인돼 있으면 **그 사용자를 로그아웃**시킨다" → 연결 후 다음 실행에서 새 익명 uid 발급, 연결 무효화 | `currentUser`가 있으면 재사용, `null`일 때만 익명 로그인 |
| 2 | `_uid`가 init 시점에 캐시됨 → 연결·전환 뒤 옛 uid로 쓰기 | `FirebaseAuth.instance.currentUser?.uid` 동적 조회 또는 `authStateChanges` 구독 |
| 3 | 웹 `mergeCloud`는 `cloudSyncOnStart`에서 시작 시 1회만 실행 → 연결/전환 뒤 재동기화 경로 없음 | `cloudResync()` 신설: `cloudLoad`→`mergeCloud`→`save()`→`submitScoreNative()`→`refreshMeta()`. `_cloudOn`·uid 갱신 포함 |
| 4 | Firestore 규칙에 `delete`가 없어 고아 문서·계정 삭제 불가 | `scores/{uid}`, `seasons/*/scores/{uid}`, `users/{uid}` 3곳에 본인 `delete` 허용 |
| 5 | 콘솔 "익명 계정 자동 정리"가 켜져 있으면 30일 미접속 익명 계정 삭제 → uid 변경, 미연결 유저 랭킹행·세이브 유실 | 콘솔에서 **OFF 확인**(현 구조에도 이미 영향) |

### 2-2. 클라우드 세이브 — ✅ 구현 완료 (Phase 2와 무관하게 먼저 들어감)
- 실제 스키마는 설계안(필드 나열)이 아니라 **웹 SAVE 통째 백업**:
  `users/{uid}: { save: {...웹 SAVE...}, updatedAt }` — 필드가 늘어도 스키마 변경이 없다.
- 닉네임도 SAVE 안에 포함 → 기기 이전 시 닉까지 복원(`nickSet`이 true일 때 클라우드 우선)
- 로컬(localStorage)이 진실원본, 클라우드는 백업. 시작 시 pull+merge → 이후 2.5초 디바운스 push
- 젬은 **폐기**(광고 전용 수익 모델 확정) — 머지 대상에서 제외
- 상세 병합 정책·푸시 가드: `FIREBASE_SETUP.md` 2-A

> ⚠️ 클라우드 세이브가 **익명 uid에 묶여 있어** 이것만으로는 재설치·기기이전 복원이
> 되지 않는다. 새 uid = 빈 문서. 복원은 위 2-1(소셜 로그인 연결)이 있어야 성립한다.

### 2-3. [신규] 계정 삭제 · 로그아웃 — 스토어 심사 필수

소셜 연결 = 계정 생성. **App Store 5.1.1(v)**와 Play 정책상 **인앱 계정 삭제**가 없으면 심사 반려.
Apple은 Sign in with Apple 사용 앱에 **토큰 revoke**까지 요구한다.

**삭제 흐름 (`deleteAccount`)**
1. 웹: 2단계 확인 모달("랭킹 기록과 클라우드 세이브가 영구 삭제됩니다")
2. 재인증 — `user.delete()`는 `requires-recent-login`을 던질 수 있으므로 연결된 provider로
   `reauthenticateWithProvider` / `reauthenticateWithCredential`
3. Apple 연결 계정이면 재인증에서 받은 `authorizationCode`로 `revokeTokenWithAuthorizationCode`
4. Firestore 본인 문서 삭제: `users/{uid}`, `scores/{uid}`, `seasons/S0..S{현재}/scores/{uid}` 루프
   (시즌 수가 유한 — 연 26개 — 하므로 Cloud Functions 없이 클라이언트에서 끝낸다)
5. `user.delete()`
6. 로컬 SAVE는 유지. 새 익명으로 계속 플레이 가능(랭킹 행은 새로 생김). 웹 `cloudResync()`.

**로그아웃 (`signOut`)** — 잘못된 계정에 연결했을 때의 탈출로.
`signOut()` → 새 익명 로그인 → 로컬 유지 → `cloudResync()`. 이전 계정 문서는 지우지 않는다(그 계정 소유자의 기록).

### 2-4. 손대는 곳 (원안 2-3 대체)
- `pubspec.yaml`: **`google_sign_in`(7.x)만 추가.** [변경] `sign_in_with_apple`은 넣지 않는다 —
  firebase_auth 내장 `AppleAuthProvider` + `linkWithProvider`가 iOS에서 네이티브로 처리(nonce 자동).
  패키지 1개 감소 = iOS Pod 충돌 리스크 감소(`FIREBASE_SETUP.md` 경고 참고).
- `google_sign_in` 7.x는 6.x와 API가 완전히 다르다: `GoogleSignIn.instance.initialize()` → `authenticate()`
  → `authentication.idToken` → `GoogleAuthProvider.credential(idToken: ...)`. accessToken 불필요.
- `lib/ranking.dart`(또는 신규 `account.dart`): init 수정(2-1a #1·#2), `linkGoogle()`, `linkApple()`,
  `signOut()`, `deleteAccount()`, already-in-use 처리(2-1 순서)
- `firestore.rules`: 본인 `delete` 허용 3곳 → **재배포 필요**
- 브리지(`lib/main.dart`):

  | 브리지 | 반환 |
  |---|---|
  | `linkAccount(provider)` — `'google'` \| `'apple'` | `{ok, switched, providers, error}` |
  | `signOut()` | `{ok}` |
  | `deleteAccount()` | `{ok, error}` |
  | `getAccountState()` **[확장]** | `{enabled, uid, linked, isAnonymous, providers: ['google.com', ...]}` |

- 웹(`assets/www/index.html`): 계정 모달(연결 버튼 2개·상태·로그아웃·삭제), `cloudResync()`,
  홈 칩 배지 승격(☁️ → 연결됨), 권유 트리거 1회, 삭제 2단계 확인
- 네이티브 설정: `FIREBASE_SETUP.md` 2-B 선행 설정(Google OAuth client 재생성, SHA-1 **3종**,
  Apple capability + entitlements, URL scheme)
- 문서: 개인정보처리방침에 "로그인 식별자(Google/Apple 사용자 ID)" 항목, App Privacy / Data Safety에 User ID 신고

### 2-5. [신규] UX · 정책
- **진입점 3곳**: 닉네임 모달 하단 "☁️ 계정 연결로 기록 지키기" 링크, 랭킹 `tierBox`,
  홈 닉 칩(연결 후 ☁️ → 연결됨 표시로 승격).
- **첫 실행에 강제하지 않는다** — 이미 닉네임 모달이 뜬다. 권유는 "잃으면 아까운 순간" **1회**만:
  첫 상점 구매 / 시즌 한정 스킨 획득 / best 갱신 중 먼저 오는 것. 이후 무음(`SAVE.linkPrompted` 플래그).
- already-in-use 병합은 묻지 않음(무손실). 토스트만.
- **스코프 최소화**: Apple·Google 모두 email·name을 요청하지 않는다(닉네임은 게임 안에 있다).
  Apple "이메일 가리기"도 무관해진다.
- 버튼 브랜딩: Apple HIG(검/백 버튼 + 로고 + "Apple로 로그인"), Google 브랜딩 가이드. 웹 HTML이라 직접 그린다.
- Analytics: `account_link {provider, result: linked|switched|cancelled|error}`, `account_delete`, `account_signout`.

### 2-6. 작업 체크리스트 (원안 2-4 대체) — 코드 ✅ 2026-09-02 / 콘솔·e2e ⬜
- [x] **콘솔(2026-09-17)**: **익명 인증 ON**(그전까지 미설정 — 랭킹·세이브가 전부 목업이었다),
      Google·Apple provider ON, 익명 자동정리 OFF, **debug** SHA-1/256 등록,
      `flutterfire configure` 재실행(`oauth_client`·`CLIENT_ID` 생성), iOS URL scheme 등록
- [ ] **콘솔 잔여**: upload 키스토어 SHA-1 · **Play App Signing SHA-1** · Apple Developer App ID capability
      (앞의 둘이 빠지면 프로덕션에서만 Google 로그인 실패)
- [x] **`firestore.rules`**: scores·seasons 본인 `delete` 허용(users는 write에 포함) — ✅ **2026-09-17 배포 + 라이브 프로브 검증**
- [x] **`ranking.dart`**: init `currentUser` 우선, uid 동적, `linkAccount(provider)`/`signOut`/`deleteAccount`,
      already-in-use 고아 삭제 후 전환(현재 사용자가 익명일 때만 — 실계정 위 전환은 `in-use-other-account`로 거부)
- [x] **브리지**: `linkAccount`, `signOut`, `deleteAccount`, `getAccountState` 확장(`appleAvailable` 추가 — Android는 Apple 버튼 숨김)
- [x] **웹**: 계정 모달, `cloudResync()`, 배지 👤/☁️/🔗, 권유 1회(홈 복귀 시 노출 — 광고·전환 화면과 겹치지 않게 큐), 삭제 2단계 확인
- [x] 목 브리지 브라우저 테스트: 시작 동기화 → 연결(전환) → 병합 → 삭제 → 권유 1회 · 브라우저 모드 무해성
- [ ] **e2e(실기기)**: 재설치 → 로그인 → 복원 / 두 기기 충돌 병합 / **앱 재시작 후 연결 유지** / 삭제 후 재연결 /
      Play 내부테스트 트랙에서 Google 로그인(App Signing SHA 검증)

---

## 권장 진행 순서
1. ✅ **Phase 1** — 닉네임 설정/변경 + 네이티브 재제출. 완료.
2. ✅ Firebase 실연동(`FIREBASE_SETUP.md`) + 클라우드 세이브. 완료.
3. ⬜ **Phase 2** — 출시 직전 또는 1차 업데이트. **2-1a의 5개 지점을 먼저 고친 뒤** 연결 기능을 올린다.
   선행 프로비저닝 절차는 `FIREBASE_SETUP.md` 2-B.

## 미해결/추후 결정
- 비속어 블랙리스트 범위(한국어 중심 최소셋 → 운영하며 확장)
- 닉 변경 빈도 제한 필요 여부(어뷰징 시 N일 1회 등) — 초기엔 무제한
- Phase 2에서 닉 변경을 서버 직접(`setNickname`)으로 옮길지, 점수 제출에 묻어 보낼지
- [신규] Android Apple 로그인(웹 플로우) 도입 시점 — Services ID·Key 등록 비용 vs iOS→Android 이전 수요
- [신규] 로그아웃으로 생기는 새 익명 랭킹 행을 그냥 둘지, 로그아웃을 "다른 계정으로 연결" 흐름으로 묶을지
- [신규] 계정 삭제 시 지난 시즌 문서 범위(S0~현재 전부 vs 최근 N시즌) — 연 26개라 전부 삭제로 시작
